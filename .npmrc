# Hoist all packages to the root node_modules so
# docs/_data/acknowledgements.data.ts can read the package.json of vite's
# (transitive) dependencies, which it resolves relative to the project root.
public-hoist-pattern[]=*
